# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'GithubVisitor' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GithubVisitor

  pod 'Alamofire', '4.9.1' # Elegant HTTP Networking in Swift
  pod 'SnapKit' # Autolayout Library
  pod 'SQLite.swift' # SQLite3 library
  pod 'PullToRefreshKit' # Pull to refresh
  # pod 'HandyJSON' # JSON parse library

  target 'GithubVisitorTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GithubVisitorUITests' do
    # Pods for testing
  end

end
